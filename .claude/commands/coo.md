Manually trigger the COO remote agent right now.

Use the RemoteTrigger tool (load it with ToolSearch select:RemoteTrigger first).
Run: action "run", trigger_id "trig_01WTWbWtBenzECs7hTENqxsC"

Confirm it fired and show the trigger name and next scheduled run time.

## Related

- [[.claude/agents/coo.md]] — COO agent definition: routing logic, delegation map, synthesis format
- [[docs/architecture.md]] — Control layer: COO sits at the top of the 7-layer model
- [[rules/agent-rules.md]] — Auto-triggers the COO enforces on every request
